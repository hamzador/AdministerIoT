<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobildev.iosm.com.priot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SittingActivity"></activity>
        <activity android:name=".loginActivity$SittingActivity" />
        <activity
            android:name=".StagairesActivity"
            android:label="@string/title_activity_stagaires"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StatisticsActivity" />
        <activity
            android:name=".PresentActivity2"
            android:label="@string/title_activity_present2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".loginActivity" />
        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>